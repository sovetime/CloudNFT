spring:
  profiles:
    active: prod
  application:
    name: @application.name@
  config:
    import: classpath:base.yml,classpath:rpc.yml,classpath:cache.yml  #这里爆红可以直接忽略，没有任何影响

server:
  port: 8087

